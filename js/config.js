/**
 * FINCA SANTA FE — config.js
 * Un solo lugar para la URL del backend.
 *
 * En desarrollo local apunta a tu servidor local (npm run dev en /backend).
 * Cuando hagamos el deploy en Render, este es el ÚNICO valor que hay que
 * cambiar (por la URL pública que te dé Render, ej: https://finca-santa-fe-api.onrender.com).
 */
const API_BASE_URL = 'http://localhost:4000';
